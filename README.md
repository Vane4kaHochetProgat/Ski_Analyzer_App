# Ski_Analyzer mobile app

### Real readme coming real soon

For now, to run the app you need to create PrivateConsts.kt in /app/src/main/java/com/example/myapplication/:

It should look like this:
```agsl
package com.example.myapplication

const val ANALYZE_BASE_URL = "http://[Ski_Analyzer address]"
const val BACKEND_BASE_URL = "http://[Database address]"
```